{
  "name": "flightwall-server",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "gen:airports": "node tools/gen-airports.js",
    "gen:airlines": "node tools/gen-airlines.js",
    "gen:device-airlines": "node tools/gen-device-airlines.js",
    "build": "esbuild src/server.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "@types/node": "^20.19.43",
    "esbuild": "^0.28.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^4.125.0"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
